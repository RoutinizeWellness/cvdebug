"use client";

import {
  BaseAutumnProvider
} from "./chunk-YHKSSPVI.mjs";
import {
  AutumnContext
} from "./chunk-3AOUWPDZ.mjs";
import {
  ConvexAutumnClient
} from "./chunk-BCATZ5OT.mjs";
import {
  AutumnClient
} from "./chunk-JISIEAGJ.mjs";

// src/libraries/react/ReactAutumnProvider.tsx
import { jsx } from "react/jsx-runtime";
var getBackendUrl = (backendUrl) => {
  if (backendUrl) {
    return backendUrl;
  }
  if (backendUrl && !backendUrl.startsWith("http")) {
    console.warn(`backendUrl is not a valid URL: ${backendUrl}`);
  }
  return "";
};
var ReactAutumnProvider = ({
  children,
  getBearerToken,
  convex,
  backendUrl,
  customerData,
  includeCredentials,
  betterAuthUrl,
  headers,
  convexApi,
  pathPrefix,
  suppressLogs
}) => {
  let client = convexApi ? new ConvexAutumnClient({
    convex,
    convexApi,
    customerData,
    headers,
    getBearerToken,
    suppressLogs
  }) : new AutumnClient({
    backendUrl: getBackendUrl(backendUrl),
    getBearerToken,
    customerData,
    includeCredentials,
    betterAuthUrl,
    headers,
    pathPrefix,
    suppressLogs
  });
  return /* @__PURE__ */ jsx(BaseAutumnProvider, { client, AutumnContext, children });
};

export {
  ReactAutumnProvider
};
